/// <reference types="node" />
/**
 * This can be used to await X nr of bytes on the client socket.
 *
 */
import { Client } from "./Client";
export declare class ByteSize {
    client: Client;
    data: Buffer;
    resolve?: Function;
    reject?: Function;
    ended: boolean;
    nrBytes?: number;
    timeoutId?: ReturnType<typeof setTimeout>;
    constructor(client: Client);
    read(nrBytes: number, timeout?: number): Promise<Buffer>;
    protected onClose: () => void;
    protected onData: (buf: Buffer) => void;
    protected end(): void;
}
